type Question {
  id: ID!
  external_id: Int!
  question: String!
  answer: String!
  value: Int
  round: Round!
  category: Category!
}

type Query {
  questions: [Question]!
  question(id: ID!): Question
}
